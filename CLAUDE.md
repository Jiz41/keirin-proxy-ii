# オートマータ・自在律 A.L.L — keirin-proxy-ii 専用設定

## プロジェクト概要
競輪予想の**完全無人化**を目的とした自動予想システム。
Renderスケジューラ（30分毎）が気象・オッズ・並び/印/SBデータを統合し、予想を自動生成してDiscordへ投稿する。

### 二つの軸
| 名称 | 種別 | 概要 |
|------|------|------|
| オートマータ | 自動版 | 完全自動化パイプライン（本ドキュメントの主対象） |
| 本家（自在律） | 手動版・WEB版 | ウィンチケット目視による手動運用 |

---

## インフラ・リポジトリ

| 項目 | 値 |
|------|----|
| オートマータURL | https://keirin-proxy-ii.onrender.com |
| GitHubリポジトリ | https://github.com/Jiz41/keirin-proxy-ii |
| Weather proxy | Hugging Face上（open-meteo経由） |
| デプロイ | Render（自動デプロイ） |
| 通知・出力 | Discord |
| GITHUB_TOKEN | Termux `.bashrc` に保存済み |

---

## アーキテクチャ概要

```
Renderスケジューラ（30分毎）
        ↓
  scheduler.js
        ↓
  ┌─────────────────────────────────┐
  │ scraper.js（二段階スクレイプ）   │
  │  ① 候補絞り込み（軽量取得）     │
  │  ② 詳細取得（対象レースのみ）   │
  └─────────────────────────────────┘
        ↓
  selector.js（予想生成）
        ↓
  weather proxy（open-meteo）
        ↓
  楽天Kドリームス向けプロキシ
        ↓
  ウィンチケット（並び/印/SBデータ）
        ↓
  Discord投稿
```

**データソース**
- 天気：open-meteo（Hugging Face weather-proxy経由）
- オッズ：楽天Kドリームス向けプロキシ
- 並び・印・SB：ウィンチケットスクレイピング

---

## 設計原則（絶対遵守）

1. **多重実行ガード必須**: scheduler.jsには必ず`isRunning`フラグを設ける
2. **二段階スクレイプ**: 全件取得→フィルタは禁止。候補絞り込み→詳細取得の順を守る
3. **ロジック一元化**: 本家とオートマータで買い目生成ロジックを分岐させない
4. **Android開発環境**: PC・開発者コンソール不可。Termux + GitHub経由が原則

---

## 障害履歴・修正記録

### メモリオーバーフロー障害（Render自動再起動）
**原因**: scheduler.jsに実行中フラグがなく、スケジュール重複起動によりメモリが線形増加。

**修正内容**:
- `scheduler.js`：多重実行ガード（`isRunning`フラグ）追加
- `scraper.js`：軽量スクレイプ関数の導入
- `selector.js`：二段階スクレイプ化（全会場フルスクレイプ廃止）

**学習**: 全レース・全会場を先にフルスクレイプしてからフィルタするアーキテクチャは非効率。二段階スクレイプでHTTPリクエストを最大約200→10以下に削減可能。

---

## 既知の課題・調査中

### 買い目差異問題
**現象**: WEB版（手動・自在律）とオートマータ出力の間に買い目の差異が発生。
**原因候補**: 本家とオートマータで買い目生成ロジックが分岐している箇所。
**方針**: 本家とオートマータの共通・分岐ロジックの全容を把握し、統合または修正。

### 競り（ジカ宣言）スクレイプ対応
**状況**: winticket の競り要素の aria-label 実値が未確認のため保留。
**対応箇所**: scraper.js の scrapeWinticket() → orchestrator.js に seris 受け渡し → lineInput に n(m) 形式で埋め込み。
競りが表示されているレースのHTMLを実取得できるタイミングで対応。

---

## UI表現ルール（ケンさんとの会話）

ケンさんへの報告・会話では必ずUI表現を使うこと。

| UI表現（使う） | 内部値（コード内のみ） |
|---|---|
| 逃先 | `'逃'` |
| 差マ | `'追'` |
| 捲り | `'自'` |
