name: keirin-weather-batch

on:
  schedule:
    - cron: '0 19 * * *'  # JST 04:00
  workflow_dispatch:

permissions:
  contents: write

jobs:
  batch:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install
      - run: bash weather/batch_runner.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
